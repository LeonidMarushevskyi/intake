:javascript
  window.org = {
    intake: {
      config: #{
        Rails.configuration.intake.merge(active_features: Feature.active_features).to_json.html_safe
      }
    }
  }

  Object.freeze(window.org.intake.config)

%meta{ charset: 'utf-8', name: 'viewport', content: 'width=device-width, initial-scale=1' }
%title
  = I18n.t :title
= stylesheet_link_tag gulp_asset_path('application.css')
= javascript_include_tag gulp_asset_path('vendor.js'), defer: true
= javascript_include_tag gulp_asset_path('application.js'), defer: true
= csrf_meta_tags
